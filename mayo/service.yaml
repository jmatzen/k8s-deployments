apiVersion: v1
kind: Service
metadata:
  name: mayo-svc
  namespace: jamchat
spec:
  type: NodePort
  selector:
    app: mayo
  ports:
    - name: web
      port: 80
      targetPort: 80
      nodePort: 30093

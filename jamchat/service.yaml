apiVersion: v1
kind: Service
metadata:
  name: jamchat-svc
  namespace: jamchat
spec:
  type: NodePort
  selector:
    app: jamchat
  ports:
    - name: web
      port: 8080
      targetPort: 8080
      nodePort: 30090
